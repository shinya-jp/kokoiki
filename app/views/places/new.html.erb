
<h2>レビュー投稿</h2>
<P>既に登録されてる場所かお調べします。</P>
		<%= search_form_for @q, url: confirm_places_path do |f| %>
			<div class="form-group row">
				<div class="col-sm-2">
					<%= f.label :name, "場所の名前", class: "col-form-label" %>
				</div>
				<div class="col-sm-5">
					<%= f.text_field :name_eq, autofocus: true, class: "form-control" %>
				</div>
			</div>

			<div class="form-group row">
				<div class="col-sm-2">
					<%= f.label :prefecture, "都道府県" %>
				</div>
				<div class="col-sm-3">
					<%= f.select :prefecture_eq, Place.prefectures.map{|k, v| [Place.prefectures_i18n[k],v]},{prompt:"選択してください"}, class: "custom-select" %>
				</div>
			</div>

			<div class="form-group row">
				<div class="col-sm-2">
					<label>ジャンル</label>
				</div>
				<div class="form-check">
					<%= f.radio_button :genre_eq, "1" %>
					<%= f.label :genre_eq, "遊び",'', value:"1", class: "form-check-label" %>
				</div>
				<div class="form-check">
					<%= f.radio_button :genre_eq, "2" %>
					<%= f.label :genre_eq, "食事", value: "2", class: "form-check-label" %>
				</div>
			</div>

			<div class="action">
				<%= f.submit %>
			</div>
		<% end %>


